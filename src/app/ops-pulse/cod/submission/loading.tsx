import { CodWorkspaceLoading } from "@/components/cod-workspace-loading";

export default function Loading() {
  return (
    <CodWorkspaceLoading
      title="Loading COD Submission"
      subtitle="Loading deposit form and recent submission register…"
    />
  );
}
