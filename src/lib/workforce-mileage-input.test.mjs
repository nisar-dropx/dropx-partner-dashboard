import test from 'node:test';import assert from 'node:assert/strict';import {parseMileageClaim} from './workforce-mileage-input.ts';
const valid=()=>{const f=new FormData();for(const [k,v] of Object.entries({workforce_id:'00000000-0000-4000-8000-000000001000',policy_id:'00000000-0000-4000-8000-000000001001',kilometres:'35.50',work_date:'2026-04-02',posting_date:'2026-04-03',reference:'TEST-CLAIM',evidence_reference:'TEST-TRIP-LOG',notes:'Synthetic journey evidence'}))f.set(k,v);return f;};
test('mileage input normalizes distance without manufacturing a rate or amount',()=>{const data=parseMileageClaim(valid());assert.equal(data.p_km,35.5);assert.equal(data.p_date,'2026-04-02');assert.equal('amount' in data,false);assert.equal('rate_per_km' in data,false);});
test('mileage input rejects invalid IDs, impossible dates and precision',()=>{for(const [key,value] of [['workforce_id','anything'],['policy_id',''],['kilometres','0'],['kilometres','1.001'],['kilometres','NaN'],['kilometres','Infinity'],['kilometres','1e3'],['work_date','2026-02-30'],['posting_date','2026-04-01'],['evidence_reference',''],['notes','ok']]){const f=valid();f.set(key,value);assert.throws(()=>parseMileageClaim(f));}});
test('mileage input preserves original work date when posting into a later period',()=>{const f=valid();f.set('posting_date','2026-05-01');const data=parseMileageClaim(f);assert.equal(data.p_date,'2026-04-02');assert.equal(data.p_posting,'2026-05-01');});
